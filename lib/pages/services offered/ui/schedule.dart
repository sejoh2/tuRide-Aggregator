import 'package:flutter/material.dart';

class ScheduleRide extends StatelessWidget {
  const ScheduleRide({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Center(child: Text('Schedule your ride'))),
      body: const Center(child: Text(('Comming soon🙂'))),
    );
  }
}
